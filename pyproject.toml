[project]
name = "customer-churn-ml"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "g", email = "g" }
]
requires-python = ">=3.14"
dependencies = [
    "jupyter>=1.1.1",
    "matplotlib>=3.11.2",
    "numpy>=2.5.3",
    "pandas>=3.0.6",
    "scikit-learn>=1.9.1",
]

[project.scripts]
customer-churn-ml = "customer_churn_ml:main"

[build-system]
requires = ["uv_build>=0.12.5,<0.13.0"]
build-backend = "uv_build"
